<%= form_for(@mymember) do |f| %>

	<h3>タイトル</h3>
	<%= f.text_field :title %>

	
	
	<h3>備考</h3>
	<%= f.text_area :body %>

	<li><%= f.label :image %></li>
	<%= f.file_field :image %> 

	<%= f.submit "送信" %>

<% end %>